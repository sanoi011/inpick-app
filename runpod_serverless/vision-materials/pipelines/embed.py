"""
CLIP/OpenCLIP image embedding (512-dim).

Phase 3 (현재): placeholder.
가이드: ViT-B/32 default → 512차원 (DB pgvector matches).
"""
from __future__ import annotations
from typing import List, Optional


def embed_image(image_path: str) -> Optional[List[float]]:
    """
    Phase 3 placeholder — None 반환 (embedding 없음).
    """
    _ = image_path
    return None
