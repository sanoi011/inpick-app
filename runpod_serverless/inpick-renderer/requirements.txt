# InPick Renderer — Phase 5 scaffold dependencies.
#
# Phase 5: 최소 의존성 (실제 diffusers 미사용, placeholder pipeline)
# Phase 6+: torch, diffusers, transformers, controlnet 등 추가

# RunPod
runpod>=1.6.0

# 이미지 처리 (control image baseline)
Pillow>=10.0.0
numpy>=1.24.0
opencv-python-headless>=4.9.0

# Phase 6+ 추가 예정 (주석 — production에 포함 시 cold start 증가):
# torch>=2.3.0
# diffusers>=0.30.0
# transformers>=4.44.0
# accelerate>=0.34.0
# controlnet-aux>=0.0.10
# safetensors>=0.4.0
